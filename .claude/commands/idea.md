---
description: Explore and refine a raw idea into a structured spec
---

Use the `idea-to-spec` skill to research and shape this idea into a spec.

Save the output to `.eko/ideas/<name>.md` where `<name>` is a slugified version of the idea.

$ARGUMENTS
