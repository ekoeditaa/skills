---
description: Execute a plan by building each phase using test-driven development
---

Read the plan from `.eko/plans/`.

MANDATORY: Load and use the `eko:plan-to-code` skill at `.claude/skills/plan-to-code/SKILL.md`. For each task, you MUST also load and use the `eko:tdd` skill at `.claude/skills/tdd/SKILL.md` and the `eko:incremental-implementation` skill at `.claude/skills/incremental-implementation/SKILL.md`. Do not improvise your own process — execute every step in each skill's Process section in order. Do not skip the Verification checklists.

$ARGUMENTS

When complete, suggest the user run `/eko:review` to review the code.
