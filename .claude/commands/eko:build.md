---
description: Execute a plan by building each phase using test-driven development
---

MANDATORY: Load and use the `eko:plan-to-code` skill at `.claude/skills/plan-to-code/SKILL.md`. Do not improvise your own process — execute every step in the skill's Process section in order. Do not skip the Verification checklist.

Read the plan from `.eko/plans/` and build it phase by phase, wave by wave.

For each task, you MUST also load and use the `eko:tdd` skill at `.claude/skills/tdd/SKILL.md` and the `eko:incremental-implementation` skill at `.claude/skills/incremental-implementation/SKILL.md`.

$ARGUMENTS

When complete, suggest the user run `/eko:review` to review the code.
