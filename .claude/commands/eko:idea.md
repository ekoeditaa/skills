---
description: Explore and refine a raw idea into a structured spec
---

Follow the process defined in the `eko:idea-to-spec` skill to research and shape this idea into a spec.

Read the skill file before starting:
- `.claude/skills/idea-to-spec/SKILL.md`

MANDATORY: Execute every step in the skill's Process section in order. Do not skip the Verification checklist.

Save the output to `.eko/ideas/<name>.md` where `<name>` is a slugified version of the idea.

$ARGUMENTS

When complete, suggest the user run `/eko:plan` to break the spec into an executable plan.
