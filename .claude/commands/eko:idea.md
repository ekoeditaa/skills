---
description: Explore and refine a raw idea into a structured spec
---

MANDATORY: Read and follow the `eko:idea-to-spec` skill at `.claude/skills/idea-to-spec/SKILL.md`. Do not improvise your own process — execute every step in the skill's Process section in order. Do not skip the Verification checklist.

Save the output to `.eko/ideas/<name>.md` where `<name>` is a slugified version of the idea.

$ARGUMENTS

When complete, suggest the user run `/eko:plan` to break the spec into an executable plan.
